<%
   @title = @release ? "Rails Contributors - #{@release.name}" : add_window_to("Rails Contributors", @time_window)
   @description = 'Ruby on Rails contributors'
%>

<h1 id="title">
  <%= @title %><br/>
  <span class="listing-total">Showing <%= pluralize @contributors.size, 'person' %></span>
</h1>
<%= link_to "See names mapping →", names_mapping_path, :class => 'name-mapping' %>

<div id="table-wrap">
  <table>
    <tr class="header">
      <th></th>
      <th>Name</th>
      <th>Commits</th>
    </tr>
    <%= render @contributors %>
  </table>
</div>

<%= render 'shared/window_sidebar' %>
